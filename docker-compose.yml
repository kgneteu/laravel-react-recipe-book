version: '3.6'

services:
  postgres:
    image: postgres:14
#    build: ./postgres
    environment:
      POSTGRES_USER: chef
      POSTGRES_PASSWORD: SUPERsecret!!!:-)
      POSTGRES_DB: recipe-book
      POSTGRES_HOST: postgres
    ports:
      - "5432:5432"

